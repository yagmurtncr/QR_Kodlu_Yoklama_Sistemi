<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_light_grey"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_gravity="center"
            android:src="@android:drawable/ic_menu_edit"
            app:tint="@color/brand_blue"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="16dp"
            android:contentDescription="Register Icon"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Hesap Oluştur"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="@color/text_main"
            android:layout_marginBottom="32dp"/>

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="20dp"
            app:cardElevation="4dp"
            app:cardBackgroundColor="@color/white">

            <LinearLayout
                android:id="@+id/registerForm"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <!-- Üniversite Seçimi -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Kurum / Üniversite"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:textColor="@color/brand_blue"
                    android:layout_marginBottom="4dp"/>
                <Spinner
                    android:id="@+id/spinnerUniversity"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:background="@drawable/gradient_background"
                    android:backgroundTint="#F1F3F4"
                    android:layout_marginBottom="16dp"/>

                <!-- E-posta -->
                <EditText
                    android:id="@+id/etEmail"
                    android:layout_width="match_parent"
                    android:layout_height="55dp"
                    android:hint="Kurumsal E-posta"
                    android:inputType="textEmailAddress"
                    android:background="@drawable/gradient_background"
                    android:backgroundTint="#F1F3F4"
                    android:padding="12dp"
                    android:layout_marginBottom="12dp"
                    android:textSize="15sp"/>

                <!-- Şifre -->
                <EditText
                    android:id="@+id/etPassword"
                    android:layout_width="match_parent"
                    android:layout_height="55dp"
                    android:hint="Şifre"
                    android:inputType="textPassword"
                    android:background="@drawable/gradient_background"
                    android:backgroundTint="#F1F3F4"
                    android:padding="12dp"
                    android:layout_marginBottom="12dp"
                    android:textSize="15sp"/>

                <EditText
                    android:id="@+id/etPasswordConfirm"
                    android:layout_width="match_parent"
                    android:layout_height="55dp"
                    android:hint="Şifre Tekrar"
                    android:inputType="textPassword"
                    android:background="@drawable/gradient_background"
                    android:backgroundTint="#F1F3F4"
                    android:padding="12dp"
                    android:layout_marginBottom="16dp"
                    android:textSize="15sp"/>

                <!-- Rol -->
                <RadioGroup
                    android:id="@+id/rgRole"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginBottom="12dp">
                    <RadioButton android:id="@+id/rbTeacher" android:text="Öğretmen" android:layout_weight="1" android:buttonTint="@color/brand_blue"/>
                    <RadioButton android:id="@+id/rbStudent" android:text="Öğrenci" android:layout_weight="1" android:buttonTint="@color/brand_blue"/>
                </RadioGroup>

                <!-- Öğrenci No -->
                <EditText
                    android:id="@+id/etStudentNumber"
                    android:layout_width="match_parent"
                    android:layout_height="55dp"
                    android:hint="Öğrenci Numarası (9 Hane)"
                    android:inputType="number"
                    android:visibility="gone"
                    android:background="@drawable/gradient_background"
                    android:backgroundTint="#F1F3F4"
                    android:padding="12dp"
                    android:layout_marginBottom="20dp"
                    android:textSize="15sp"/>

                <androidx.appcompat.widget.AppCompatButton
                    android:id="@+id/btnRegister"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:text="HESABI OLUŞTUR"
                    android:textColor="@color/white"
                    android:background="@drawable/gradient_background"
                    android:backgroundTint="@color/brand_blue"
                    android:textStyle="bold"
                    app:cornerRadius="12dp"/>

            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/tvBackToLogin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Zaten hesabınız var mı? Giriş yapın"
            android:textColor="@color/brand_blue"
            android:layout_gravity="center"
            android:layout_marginTop="24dp"
            android:padding="12dp"
            android:textStyle="bold"/>

    </LinearLayout>
</ScrollView>